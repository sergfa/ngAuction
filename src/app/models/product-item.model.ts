export class ProductItem {
    
    constructor(public id: number, public price: number,public title: string, public description: string, public rating: number, public categories: string []){   
    }
}